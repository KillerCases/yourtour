
<%= bootstrap_form_for(@tour_city) do |f| %>

  <% if @tour_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tour_city.errors.count, "error") %> prohibited this tour_city from being saved:</h2>

      <ul>
      <% @tour_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :city, placeholder: "Enter city name" %>
<%= f.text_field :country, placeholder: "Enter country name" %>
  <%= f.submit %>
<% end %>

