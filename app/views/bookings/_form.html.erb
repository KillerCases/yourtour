<%= bootstrap_form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.collection_select :calendar_id, @calendars, :id, :calendar_datetime,{:selected => @calendar_selected.id},{:prompt => true}%>
  <%= f.select :count_adult, options_for_select(1..50), {:prompt => true},{:count_adult => 'doctor'} %>
  <%= f.select :count_child, options_for_select(0..50), {:prompt => true} %>

  <%= hidden_field_tag :calendar_id, params[:calendar_id] %>

  <%= f.submit %>
<% end %>

