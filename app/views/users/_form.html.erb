<%= bootstrap_form_for(@user) do |f| %>

<% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this tour from being saved:</h2>

        <ul>
          <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

<%= f.text_field :name, placeholder: "Enter user name" %>
<%= f.text_field :email, placeholder: "Enter user email" %>
<%= f.collection_select :role, User::ROLES, :to_s, :titleize %>


  <%= f.submit "Save" %>
<% end %>
